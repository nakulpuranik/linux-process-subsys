/**
*
*  Linux system programming
*  Process Subsystem
**/

Assignment : Write a program which create three level process hierarchy where process 1 creates process 2 and it internally creates process 3.

Description :
This code creates three level process hierarchy. 
System call used which are fork(), exec().

NOTE: processHierarchy.c file should be ignored for now, was trying to pass the same executable for the sublevel hierarchy. and is work in pending.